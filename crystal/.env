# Data source configuration
# Set to either:
# - A folder path for filesystem mode (e.g., /docs or /data)
# - An API URL for API mode (e.g., https://api.example.com/docs)

# Filesystem mode (default):
VITE_SOURCE=/docs

# API mode example:
# VITE_SOURCE=https://api.example.com/docs

# App mode: 'dev', 'preview', or 'production'
# dev: Shows editor for content modification + settings sidebar
# preview: Read-only view + settings sidebar (can toggle to dev)
# production: Read-only view without settings sidebar (pure production)
VITE_MODE=production

# Backend API URL (where the Go server is running)
VITE_BACKEND_URL=http://localhost:8080/api
